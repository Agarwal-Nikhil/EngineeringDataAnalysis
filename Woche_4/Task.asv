clc
clear all;
load 'Timber.dat';
E = Timber(1:50,5); %YoungsModulus, N/mm^2
P = 2000; %N
L = 4000; %mm
b = 150; %mm
h = 250; %mm
I = b*(h^3)/12;


%Fitting logn to mu and var
param = lognfit(E);

mu=param(1);
var=param(2);

seed=normrnd(mu,var,1,1000)
u = P*(L^3)./(3*seed*I);

figure(1)
subplot(1,2,1)
hist(ws_denmark,10)
title('Dist')
subplot(1,2,2)
hist(ws_norway,10)
title('Norway')

